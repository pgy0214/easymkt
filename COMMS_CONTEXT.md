# 통신 / 외부 API 키 셋업 — 작업 로그

이 문서는 **통신 관련 작업(문자 SMS, 카카오톡, 기타 외부 API 키 셋업)**을 전담하는 별도
Claude Code 세션(데스크톱)의 작업 로그입니다. 프로젝트 전체 맥락은 `PROJECT_CONTEXT.md`를
먼저 읽으세요 — 이 문서는 그중 "통신/API 키" 영역만 날짜순으로 기록합니다(번호를 매기지 않고
항상 파일 맨 아래에 추가하는 방식 — 다른 세션과 동시 편집이 겹쳐도 충돌 위험이 거의 없게
하기 위함).

## 2026-08-08

- **알리고(Aligo) SMS 활성화.** `app/sms.py`에 SMS(`send_sms`/`send_otp_sms`)와 카카오
  알림톡(`send_kakao_alimtalk`) 발송 코드는 이미 구현돼 있었으나(PROJECT_CONTEXT.md 항목 58),
  `.env`의 `ALIGO_USER_ID`/`ALIGO_API_KEY`/`ALIGO_SENDER`가 비어 있어 실제 발송이 막혀 있었음
  (PROJECT_CONTEXT.md "아직 안 된 것"에 2026-08-02 보류 지정돼 있던 항목). 사용자가 알리고
  "신청/인증" 페이지에서 자격증명을 확인해 제공, `.env`에 채워 넣음 — 발신번호는 테스트용
  `010-5014-0735`(실서비스 시작 시 다른 번호로 교체 예정).
- **IP 화이트리스트 유의사항.** 알리고 계정에 등록된 발송 서버 IP는 `3.36.0.87`,
  `112.175.85.194` 두 개뿐. 지금은 이 데스크톱(로컬)에서 백엔드를 돌리는데, 이 컴퓨터의
  실제 외부 IP가 등록 목록에 없으면 알리고가 요청을 거부할 수 있음 — 발송 실패 시 가장 먼저
  확인할 것. 나중에 클라우드로 옮길 계획도 있어 그때는 클라우드 서버 IP를 별도로 등록해야 함.
- **카카오는 아직 범위 미정.** 카카오 로그인(OAuth, `app/kakao.py`)과 카카오 알림톡(Aligo
  경유, `send_kakao_alimtalk`) 둘 다 자격증명 미설정 상태로 보류. 사용자가 용도(어떤 상황에
  누구에게 무슨 메시지를 보낼지)를 정하면 이어서 진행.

- **테스트 발송 성공.** 처음엔 `-101 허용되지않은 IP` 에러로 실패 — 이 컴퓨터의 실제 외부 IP
  (`124.53.147.105`)가 알리고에 등록돼 있지 않았던 것. 사용자가 알리고 "신청/인증" 페이지에서
  해당 IP를 발송 서버 IP로 등록한 뒤 재시도, `010-5014-0735`로 테스트 문자 발송 성공 확인
  (`result_code: 1`), 사용자가 실제 휴대폰 수신까지 직접 확인함. **주의**: 이 컴퓨터가 다른 네트워크로 나가면(다른 와이파이, 이동통신
  테더링, 나중에 클라우드로 이전 등) 외부 IP가 바뀌어 다시 이 에러가 날 수 있음 — 발송 실패
  시 가장 먼저 `curl https://api.ipify.org`로 현재 IP를 확인하고 알리고에 등록됐는지 볼 것.

## 2026-08-17

- **Railway/Vercel 프로덕션 배포 완료.** 백엔드는 Railway(`review-managing-production.up.railway.app`),
  프론트는 Vercel(`review-managing.vercel.app`)에 배포됨. 알리고 SMS 자격증명은 Railway
  "Variables" 탭에도 별도 등록 필요(로컬 `.env`와 완전히 별개) — 이미 등록돼 있음을 확인.
  Railway가 기본적으로 아웃바운드 IP를 고정하지 않아 알리고 IP 화이트리스트에 계속 막혔던
  문제는, Railway Pro 플랜으로 업그레이드 후 "Static Outbound IPs"를 켜서 발급받은 IP 3개
  (`162.220.232.251`, `152.55.177.192`, `152.55.177.193`)를 알리고에 전부 등록해 해결.
  이후 프로덕션 백엔드로 직접 OTP 발송 테스트(`/api/portal/otp/request`) → 실제 문자 수신까지
  확인 완료.
- **[별개 이슈, 해결됨] 프로덕션 버튼 안 보임 버그.** 리뷰어 포털 등에서 브랜드 색상
  (`bg-brand-500` 등)을 쓰는 버튼들이 투명하게 렌더링돼 아예 안 보이던 문제 발견 — 원인은
  "토스 스타일" 리디자인(다른 세션, 리뷰개발 노트북 진행 중)에서 색상 테마 정의
  (`review-manager/src/index.css`의 `@theme` 블록)만 커밋이 안 된 상태였음. 이미 배포된
  Portal.jsx 등은 이 색상 클래스를 이미 쓰고 있었는데 정의가 없어서 전부 투명 처리된 것.
  다른 세션이 진행 중인 나머지 45개 파일(컴포넌트 리디자인 자체)은 건드리지 않고,
  `index.css`의 색상 토큰 정의 부분만(순수 추가, 기존 blue-*/gray-* 사용처에 영향 없음)
  분리해서 커밋·푸시 — Vercel 재배포 후 버튼 색상 정상 렌더링 확인.

## 담당 범위 (진행 중)
- 문자(SMS)/카카오톡 발송 관련 API 키·설정
- 기타 외부 서비스 API 키 셋업 전반

새 세션에서 이 프로젝트를 열었다면, 통신/API 키 관련 최신 상태는 이 파일 맨 아래 항목을
확인하세요.
