# LOGIC.md 이전 버전 백업

`LOGIC.md`에서 대체된 예전 규칙을 보관합니다. 최신 규칙은 `LOGIC.md`를 보세요.

## [2026-08-21] 관리자 로그인 — DB 기반 다중 관리자로 전환되기 전 버전

- `/admin`은 로그인 안 된 상태로 들어가면 로그인 폼 없이 **"권한이 없습니다"**
  안내문구만 보여준다. 관리자 로그인 자체는 이 경로에서 할 수 없다.
- 대신 `/`(리뷰어 로그인 폼) 또는 `/ads`(광고주 로그인 폼)에 관리자 아이디/비밀번호를
  입력하면, 해당 포털 로그인이 실패한 뒤 자동으로 관리자 로그인을 한 번 더
  시도하고, 성공하면 `/admin`으로 자동 이동한다.
- (이 부분 자체는 최신 버전에도 그대로 유지됨 — 바뀐 건 "관리자 로그인"이
  .env 단일 계정 확인이었다는 점.)

## [2026-08-21] 테스트 계정 — admin/1234 적용 보류하던 버전

매 배포/재시작마다 아래 계정이 없으면 자동으로 생성한다(있으면 손대지 않음,
`crud.seed_test_accounts`):
- 광고주: 아이디 `ads1` / 비밀번호 `1234`
- 리뷰어: 아이디 `review` / 비밀번호 `1234`
- **관리자(admin/1234)는 아직 미적용** — 실제 서비스 마스터 계정 비밀번호를
  "1234"로 두는 건 보안 위험이 커서, 사용자 확인 없이 적용하지 않고 있음
  (별도로 해시값 전달 예정, 사용자가 원하는 실제 비밀번호를 정하는 게 안전).

→ 대체 이유: 사용자가 명시적으로 "관리자=admin/1234, 개발자=kingsas/chlrkd12!@,
둘 다 category='admin'으로, 실제 오픈 전 테스트 편의용"으로 지시함. 또한 기존
category='admin'이 "회사 자체보유(리뷰포스팅) 계정"이라는 다른 뜻으로 이미
쓰이고 있던 걸 발견해서, 그 뜻은 'own'으로 옮기고 'admin'을 대시보드 관리자
전용으로 재정의함.
