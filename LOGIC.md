# 서비스 규칙 (LOGIC)

이 문서는 사용자가 대화 중에 직접 지시한 **동작 규칙**만 모아둔 스펙입니다.
"무슨 일이 있었는지"(변경 이력)는 `PROJECT_CONTEXT.md`에, "통신/API 키"는
`COMMS_CONTEXT.md`에 있고, 이 파일은 "지금 어떻게 동작해야 하는가"만 다룹니다.

**규칙을 바꿀 때는:** 기존 항목을 바로 지우지 않고 `LOGIC_backup.md`로 옮긴 뒤,
새 규칙을 여기에 적기 전에 먼저 사용자에게 의견을 제안한다(왜 바꾸는 게 맞다고
생각하는지 설명하고 확인받은 뒤 반영).

## 도메인 구조
- `/` (메인 도메인) → 리뷰어 포털 (기본 진입점)
- `/ads` → 광고주 포털
- `/admin` → 관리자 대시보드
- 기존 `/portal`, `/advertiser` 경로는 호환 없이 완전히 제거됨(요청에 따라).

## 관리자 개념 — "관리자"(admin) vs "자체보유계정"(own)
- `reviewers.category`의 `'admin'`은 **"/admin 대시보드에 로그인 가능한 사람"**을
  뜻한다(이지리뷰를 실제로 운영하는 사람 — 관리자, 개발자 등). 리뷰 포스팅용으로
  회사가 자체 보유한 계정(예전엔 이것도 `'admin'`이라 불렀음)은 **`'own'`**으로
  이름이 다르다 — "관리자 계정" 화면(`AdminAccountManager.jsx`)에서 관리하는
  대상이 바로 이 `'own'` 쪽이고, 회원관리(`MemberManager.jsx`)에서 관리하는
  "관리자"는 `'admin'` 쪽이다. 두 개념을 섞지 않는다.
- `/admin` 대시보드 로그인은 `reviewers` 테이블 기반이다(`category='admin'` +
  `username`/`password_hash` + `is_active=True`) — 여러 명이 각자 다른 아이디로
  로그인 가능하고, 회원관리에서 "비활성화"하면 그 사람은 즉시 대시보드 로그인이
  막힌다(킬스위치). 예전처럼 `.env`에 한 명만 하드코딩된 방식이 아니다.
- `/admin`은 로그인 안 된 상태로 들어가면 로그인 폼 없이 **"권한이 없습니다"**
  안내문구만 보여준다. 로그인 자체는 `/`(리뷰어 로그인 폼) 또는 `/ads`(광고주
  로그인 폼)에 관리자 아이디/비밀번호를 입력하면, 그 포털 로그인이 실패한 뒤
  자동으로 관리자 로그인을 한 번 더 시도하고, 성공하면 `/admin`으로 이동한다.
- 이미 `admin_token`이 있는(로그인된) 상태의 세션은 이 변경과 무관하게 계속
  `/admin` 대시보드에 그대로 들어간다 — 로그아웃되지 않는다.
- 지금은 `'admin'`으로 등록된 계정끼리 권한 차이가 전혀 없다(관리자든 개발자든
  대시보드 전체에 동일하게 접근 가능) — 나중에 계정별로 권한을 나누고 싶어지면
  이 구조(DB 행 기반) 위에 권한 필드를 추가하는 방식으로 확장 가능하도록만
  열어둔 상태.

## 전화번호 인증 (회원가입/비밀번호 찾기) — 보안
- **회원가입 시도인데 그 전화번호가 이미 다른 계정에 등록돼 있으면, 자동으로
  임시 비밀번호를 발급하거나 로그인시키면 안 된다.** 대신 그 계정의 카테고리에
  맞는 안내 문구만 보여주고 멈춘다:
  - 리뷰어로 가입된 번호: "이미 리뷰어로 가입된 번호입니다. 추가문의는 관리자에게 부탁드립니다."
  - 광고주로 가입된 번호: "이미 광고주로 가입된 번호입니다. 추가문의는 관리자에게 부탁드립니다."
- **비밀번호 찾기는 전화번호 인증만으로 끝나면 안 되고, 추가로 아이디까지
  일치해야 임시 비밀번호를 내준다** (전화번호 하나만 알면 아무 계정이나 비밀번호를
  리셋할 수 있는 상태였던 걸 막기 위함 — 인터넷 서비스에서 흔히 쓰는
  "본인인증 + 아이디 확인" 2단계 방식).
- (구현 위치: `Portal.jsx`/`Advertiser.jsx`의 `handleVerify`, 백엔드
  `/api/portal/otp/verify`·`/api/portal/me/reset-password`는 변경 없이 프론트에서
  자동 리셋 호출을 제거하고 아이디 확인 단계를 추가하는 방식으로 구현)

## 회원가입 폼 — 세부 UX 규칙
- 아이디/비밀번호 입력창은 브라우저 자동완성(이전 로그인 정보 자동채움)이
  뜨면 안 된다 (`autoComplete="off"` / `"new-password"`).
- 개인정보 수집·이용 동의(필수)와 별개로 **마케팅 정보 수신 동의(선택)** 체크박스도
  항상 포함한다.
- 파일 첨부 입력창은 브라우저 기본 문구("파일 선택 선택된 파일 없음")를 그대로
  쓰지 않는다 — 커스텀 버튼으로 "파일 선택"만 보여주고, 파일을 고르면 파일명만
  표시한다.

## 크롤러 / 영수증 생성 (클라우드 배포)
- 크롤러(매장정보 자동입력 등)는 Railway에 Dockerfile로 크로미움을 설치해서
  클라우드에서도 동작하게 한다 (`CHROME_BINARY_PATH`/`CHROMEDRIVER_PATH` 로 지정).
- 영수증 이미지 생성 폰트는 원래 쓰던 상업 폰트(AppleSDGothicNeoL/Arial) 대신
  무료 오픈소스 대체 폰트를 리포지토리에 번들해서 쓴다 — 한글: **Pretendard**,
  숫자: **Roboto** (사용자가 직접 비교해서 확정).
- 상업 폰트 파일 자체를 리포지토리에 복사해 넣는 것은 라이선스 위반이라
  요청해도 하지 않는다.
- 로컬(크롬/폰트가 있는 환경)에서 만든 영수증 이미지를 온라인 시스템에 수동
  업로드할 수 있는 경로도 있다(`POST /api/tasks/{id}/receipt-image`).

## 테스트 계정 (항상 DB에 존재해야 함)
매 배포/재시작마다 아래 계정이 없으면 자동으로 생성한다(있으면 손대지 않음,
`crud.seed_test_accounts`) — 전부 `category='admin'`으로 로그인해야 하는
두 계정을 포함해서, 실제 오픈 전 테스트 편의를 위해 일부러 단순한 비밀번호로
시작한다. 실제 서비스를 열기 전에 회원관리 화면에서 진짜 정보(아이디/비밀번호
등)로 바꿔야 한다:
- 관리자: 아이디 `admin` / 비밀번호 `1234`
- 개발자: 아이디 `kingsas` / 비밀번호 `chlrkd12!@`
- 광고주: 아이디 `ads1` / 비밀번호 `1234`
- 리뷰어: 아이디 `review` / 비밀번호 `1234`

## 배포/커밋 프로세스
- 커밋하기 전에 항상 **클린 워크트리 기준으로** 백엔드 임포트 + 프론트 빌드 +
  주요 API 엔드포인트를 실제 데이터로 재현해서 검증한다 — 문제가 터진 뒤
  고치는 게 아니라 미리 걸러낸다.
- 어떤 파일을 커밋할 때 그 파일에 내가 의도한 것보다 훨씬 큰 diff(다른 세션의
  미커밋 작업 등)가 딸려 들어가면, 커밋하기 전에 크기를 확인하고 사용자에게
  미리 알린다.
- GitHub에 푸시하기 전에는 항상 사용자에게 확인을 받는다(매번, 이전에
  허락받았어도 자동으로 넘기지 않음).
